# Bit2AtomBot 项目介绍

> 一款运行在浏览器里的笔式绘图仪控制系统 —— 基于 Web 的 AxiDraw 全流程控制端
>
> 本介绍对应最新版本 **v0.20.0**（2026-09-08）

---

## 1. 项目是什么

**Bit2AtomBot** 是一款专为笔式绘图仪（Pen Plotter）设计的现代化 Web 控制系统，支持 AxiDraw V3 / Brushless、NextDraw 2234、iDraw H SE 以及任意兼容 EBB 固件的设备（包括按自定义传动参数构建的自制绘图仪）。

它将绘图仪的控制从传统桌面软件（Inkscape 插件）中解放出来：**只需浏览器 + Node.js**，即可完成从 SVG 加载、路径预览、排版缩放调整到设备驱动的全流程操作。局域网内的手机、平板、笔记本都能通过浏览器直接操控绘图仪，树莓派 Zero 也能流畅运行服务端。

一句话概括它的三个核心能力：

1. **画得对** —— SVG 变换、真实尺寸、排版缩放的渲染与解析达到"所见即所得"
2. **画得稳** —— 面向数小时长时任务的全链路可靠性设计（超时保护、断连自恢复、速率钳制、任务日志）
3. **画错了能救** —— 暂停回溯重绘与区间补画，漏画无需废弃整幅作品

---

## 2. 项目起源：与 SAXI 的关系

本项目以开源项目 [SAXI](https://github.com/alexrudd2/saxi)（AGPL-3.0）为基础进行深度改造。SAXI 提供了优秀的整体框架——Express + WebSocket 服务端、React 预览界面、EBB 串口协议层（LM/XM 指令自适应、运动 FIFO 管理）、恒加速度运动规划、路径预处理（排序/合并/过滤）、纸张尺寸体系、图层控制、无硬件模拟与 CLI 批处理——本项目**完整保留**该框架，并针对实际绘图中的真实痛点做了系统性扩展与工程化改造。

### 深度改造总览

| 改进维度 | SAXI | Bit2AtomBot |
| --- | --- | --- |
| **渲染正确性** | SVG transform 被静默丢弃，图形比例失真 | 自研矩阵引擎，6 种变换函数任意深度复合，与浏览器原生结果精度一致（1e-9） |
| **绘制容错** | 仅暂停/继续，漏画只能整图重画 | 暂停回溯重绘 + 区间补画，红色标记保留供质量检查 |
| **归位可靠性** | 依赖 EBB `HM` 命令（电机开关循环后误判原点） | 基于应用层位置的抬笔行程归位、通信探活 + 队列清空重试、全命令超时 |
| **防撞轴** | 无 | 服务端超界校验硬拒绝 + 预览红色遮罩警示，支持自定义硬件工作区 |
| **缩放与排版** | 仅适配页面 | 三态缩放（等比/1:1/自定义比例）+ 九宫格排版锚点 + 裁剪至边距 |
| **绘制质量** | 无隐藏线处理 | 隐藏线去除，模拟真实遮挡 |
| **成果输出** | 无 | 导出经路径优化与隐藏线去除后的"所见即所得"SVG |
| **硬件适配** | 固定 3 款预设 | 自定义传动参数（步距角/细分/齿数/齿距/工作区），配置持久化 |
| **运行依赖** | 内置云端生图（需 API Key、外网） | 纯本地运行，无隐私外传 |
| **界面** | 英文 | 全中文、暗色主题、网格标尺、实时进度统计 |
| **质量保障** | 20 个测试用例 | 53 个测试用例（9 个文件）+ biome lint 零告警 |
| **可追溯性** | 无任务日志 | 按源文件归档的绘制任务日志 + 服务端运行日志落盘 |

---

## 3. 核心功能

### 3.1 输入与渲染（画得对）

- **SVG 拖放加载** —— 拖入或粘贴 SVG 文件，自动解析展平为路径
- **transform 完整支持** —— `matrix`/`translate`/`scale`/`rotate`/`skewX`/`skewY` 全部 6 种变换函数，任意深度 `<g>` 嵌套复合，含复合路径（单 `<path>` 多 `M` 子路径）的文件（Affinity Designer 典型导出格式）变换不再丢失。无需预先用 Inkscape 烘焙变换
- **SVG 真实物理尺寸自动检测** —— 导入时按根元素 `width` 的绝对物理单位（mm/cm/in/pt/pc/q）或 px 与 viewBox 的比值推算真实尺度，非 96dpi 导出的文件也能按原尺寸正确绘制
- **纸张与排版设置** —— 内置数十种预设纸张（A/B/Arch/US 系列等）+ 自定义宽高（mm）、横竖版、可调边距；九宫格排版锚点（水平左中右 × 垂直上中下）+ 自定义 X/Y 偏移
- **缩放模式三态** —— 「等比缩放到纸张」（默认）/「按原尺寸绘制 (1:1)」/「自定义缩放比例」三选一；非等比模式可配合「裁剪至边距」，按线段与边距框求交精确裁剪，区域内节点与线段完整保留
- **实时预览** —— 纸张模拟区域内预览路径，按绘制顺序着色，5/10mm 网格与四周刻度标尺辅助定位

### 3.2 绘制执行（画得稳）

- **完整运动规划** —— 恒加速度梯形/三角形速度曲线、拐角速度优化、路径排序（最小化抬笔空跑）、路径合并、短路径过滤、去重点；规划在 Web Worker 中后台执行，复杂图形不阻塞界面
- **图层控制** —— 按 stroke 颜色或 group ID 自动分层，选择特定图层绘制
- **工作范围校验（防撞轴）** —— 绘制前服务端扫描计划全部坐标（含落笔路径、抬笔空程与首尾行程），超出机械行程即以 400 拒绝并提示超界方向与数值；预览中超界区域红色遮罩 + 底部警示条。内置硬件（v3/brushless 430×300 mm，nextdraw-2234 559×864 mm）与配置了工作区的自定义硬件均参与校验
- **隐藏线去除** —— 自动裁剪填充图形后方的线条，模拟真实遮挡效果
- **SVG 导出** —— 导出经路径优化与隐藏线去除后的成果文件（仅含落笔路径）
- **模拟绘制** —— 无硬件时按真实速度曲线逐动作模拟，十字准星沿路径实时移动

### 3.3 漏画补救（画错了能救）

**暂停回溯重绘** —— 绘制中途因出墨不畅漏画时：暂停 → 拖动滑块回溯到任意一条路径（预览橙色高亮待重绘区间）→ 绘图仪抬笔安全移动到回溯点重绘后续内容（线条随进度由橙转红）。支持连续多次回溯、叠加记录，回溯以路径为最小单位保证静止起步。服务端模式与 WebSerial 直连模式均支持。

**补画模式** —— 整幅绘制结束后发现局部漏画：双滑块选择路径区间 → 仅补画选中区间 → 完成后自动抬笔回到起始点。补画区间可多次叠加执行，所有补画区域以红色标记保留，直到开始新绘制或清除 SVG。

绘制完成后红色重绘标记持续保留（不自动复位），便于对照检查重复绘制区域的最终质量。

### 3.4 可靠性工程（长时绘制保障）

面向长达数小时的绘制任务，项目沉淀了一整套可靠性设计：

- **全命令超时保护** —— 所有 EBB 命令均带超时（常规 15s，长行程按钳制后估计时长动态计算 + 60s 裕量），超时后清空命令队列、等过 500ms 沉降期、抬笔兜底、断使能并弹窗提示，界面不卡死
- **LM 速率钳制** —— 运动速率自动钳制 ≤24999 步/s，防止固件 32 位相位累加器溢出导致运动引擎停摆
- **深 FIFO 排空超时动态计算** —— 高密度图形绘制结束时按计划时长 + 60s 裕量等待设备排空积压，杜绝"电机未归位"误报
- **串口写入错误自恢复** —— USB 瞬断（错误码 31）等写入失败自动注入命令队列中止绘制，配合 `unhandledRejection` 兜底，服务不再静默崩溃
- **归位全流程加固** —— 补画后基于应用层已知位置的抬笔行程归位（`LM` 序列，规避 EBB `HM` 在电机关-开循环后失效的固件行为）；归位前 `QM` 探活，队列卡死自动清空重试；分步耗时统计（probe/pen/motors/travel/idle/disable）可作为硬件健康参考
- **断连保护** —— 浏览器直连模式下 USB 拔出立即中止挂起命令并退出绘制状态，不会卡在"绘制中"
- **双重日志体系** —— 服务端运行日志（`logs/bit2atombot-<日期>-<时间>.log`，自动保留 50 个）记录归位分步耗时、通信探活等性能数据；绘制任务日志（`logs/[源文件名]__<时间戳>.log`）按文件归档完整记录任务头（硬件/FIFO/预计时长/落笔距离/最大速度）、进度心跳、暂停/恢复/回溯事件与任务尾（实际时长/距离/结束状态），便于项目管理与故障诊断

---

## 4. 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      浏览器（React UI）                       │
│                                                             │
│  ui.tsx ──→ plan.worker.ts ──→ background-planner.ts        │
│    │         （Web Worker 后台规划，避免阻塞 UI）              │
│    ├─ SVG 解析（readSvg + 自研 transform 矩阵引擎）           │
│    ├─ 路径预处理（massager.ts：缩放/排版/裁剪/隐藏线/排序）     │
│    ├─ 运动规划（planning.ts：恒加速度速度曲线）                │
│    └─ 预览渲染（着色/进度/回溯高亮/网格标尺/超界遮罩）          │
│                                                             │
│  ┌──────────── 服务端模式（默认）────┐  ┌── WebSerial 模式 ──┐ │
│  │ Bit2AtomDriver (HTTP + WS)      │  │ WebSerialDriver    │ │
│  └───────────────┬─────────────────┘  └────────┬───────────┘ │
└──────────────────┼─────────────────────────────┼─────────────┘
                   │ ws:// :9080                 │ navigator.serial
┌──────────────────▼─────────────┐  ┌───────────▼─────────────┐
│   server.ts（Express + WS）     │  │   浏览器直连 USB         │
│   /plot /pause /resume /cancel │  │   （IS_WEB=1 构建）      │
│   doPlot：可重定位执行循环       │  └───────────┬─────────────┘
└──────────────────┬─────────────┘              │
                   │  Node SerialPort            │
                   ▼                             ▼
        ┌────────────────────────────────────────────┐
        │            ebb.ts（EBB 协议层）              │
        │   LM/XM 指令按固件版本自适应、FIFO 深度管理    │
        └────────────────────┬───────────────────────┘
                             ▼  USB 串口
                   EBB 控制板 / 绘图仪
```

### 双驱动模式

| | 服务端模式（默认） | WebSerial 模式 |
| --- | --- | --- |
| 驱动类 | `Bit2AtomDriver` | `WebSerialDriver` |
| 通信 | Express HTTP + WebSocket + NodeSerialPort | Chrome/Edge `navigator.serial` |
| 适用 | 树莓派/服务器部署、手机平板远程访问 | 免 Node.js，纯浏览器控制 |

另有第三种模式：直接向设备发送单条指令（不经 Web 界面），用于开发与测试。

### 关键数据流

1. **加载** —— SVG 文本 → `readSvg()`（flatten-svg 展平 + 自研变换矩阵应用）→ `Path[]`
2. **规划**（Web Worker）—— 图层过滤 → 隐藏线去除 → 路径排序/合并/去重 → 缩放/排版/裁剪（massager.ts，每条路径携带原始索引贯穿全部变换）→ 毫米→步数换算 → 生成 `Plan`
3. **执行** —— 驱动层逐动作下发 EBB 指令，实时回报进度
4. **回溯/补画** —— 暂停态下发回溯目标 → 计算安全移动 → 重放后续动作

---

## 5. 技术栈

| 层 | 技术 |
| --- | --- |
| 前端 | React 19 + TypeScript + esbuild，Web Worker 后台规划 |
| 服务端 | Node.js ≥ 20 + Express 5 + WebSocket（ws） |
| 串口 | serialport 13（服务端）/ WebSerial API（浏览器直连） |
| 图形处理 | flatten-svg（SVG 展平）、optimize-paths（路径优化）、polygon-clipping（隐藏线裁剪）、自研 transform 矩阵引擎 |
| 质量工具 | vitest（53 用例）、biome（lint 零告警）、TypeScript 5 |

---

## 6. 项目结构

```
Bit2AtomPlotWebUI/
├── cli.mjs                    # CLI 入口（Web 服务 / 批处理绘制）
├── index.mjs                  # 包入口（bin: bit2atombot → cli.mjs）
├── build.mjs                  # 前端构建脚本（esbuild）
├── package.json               # 项目配置（依赖、脚本、bin）
├── tsconfig.json              # TypeScript 基础配置
├── tsconfig.server.json       # 服务端编译配置
├── tsconfig.ui.json           # 前端编译配置
├── biome.json                 # 代码检查（biome）配置
├── vitest.config.ts           # 测试配置
├── LICENSE                    # AGPL-3.0 许可证
├── README.md                  # 项目说明与功能清单
├── PROJECT_INTRODUCTION.md    # 项目介绍（本文件）
├── CHANGELOG.md               # 版本更新日志
├── ARCHITECTURE.md            # 架构说明
├── TECHNICAL_REPORT.md        # 技术报告（与 SAXI 对照）
├── test-hidden-line.svg       # 隐藏线去除测试文件
├── .github/workflows/         # CI（lint.yml / publish.yml）
├── docs/                      # docs/RELEASE.md 发布流程、界面截图
├── tools/                     # tools/release.mjs 发布脚本、file-edit.py / safe-edit.ps1 编辑工具
├── src/
│   ├── ui.tsx                 # React 主界面（预览/回溯/排版/硬件配置）
│   ├── style.css              # 界面样式（暗色主题/网格标尺）
│   ├── index.html             # HTML 模板
│   ├── bit2atomLogo.svg       # 品牌 Logo
│   ├── icons/                 # SVG 图标（rotate-drawing 等）
│   ├── server.ts              # Express 服务器（/plot /pause /resume /cancel）
│   ├── cli.ts                 # CLI 参数解析
│   ├── drivers.ts             # 驱动抽象层（服务端 / WebSerial 双实现）
│   ├── serialport-serialport.ts  # Node SerialPort 串口适配
│   ├── ebb.ts                 # EBB 串口协议（LM/XM 自适应、FIFO、超时）
│   ├── planning.ts            # 运动规划内核（速度曲线、回溯算法）
│   ├── massager.ts            # 路径预处理（缩放/排版/裁剪/隐藏线集成）
│   ├── background-planner.ts  # Web Worker 后台规划调度
│   ├── plan.worker.ts         # Web Worker 规划入口
│   ├── hiding.ts              # 隐藏线去除算法
│   ├── export-svg.ts          # Plan → SVG 成果导出
│   ├── plot-log.ts            # 绘制任务日志（按源文件归档）
│   ├── run-log.ts             # 服务端运行日志落盘
│   ├── paper-size.ts          # 纸张尺寸定义
│   ├── util.ts                # 工具函数
│   ├── vec.ts                 # 2D 向量运算
│   ├── useComponentSize.ts    # 组件尺寸 Hook
│   ├── global.d.ts            # 全局类型声明
│   └── __tests__/             # 9 个测试文件（53 个用例）+ fixtures/ + mocks/
├── dist/                      # 编译产物（server / ui，构建生成）
└── logs/                      # 运行日志与绘制任务日志（运行时自动生成）
```

---

## 7. 快速上手

### 环境要求

- Node.js ≥ 20.0.0、npm ≥ 9
- Chrome / Edge / Firefox 任意现代浏览器
- Windows 10/11 / macOS / Linux（含树莓派全系列）

### 启动

```bash
cd Bit2AtomPlotWebUI
npm install
npm run build          # 构建服务器 + 前端
node cli.mjs           # 启动服务，浏览器打开 http://localhost:9080
```

日常使用 `npm start`（完整构建 + 启动）或 `node cli.mjs`（跳过构建快速启动）。

### 基本使用（3 步）

1. 将 SVG 文件**拖入**预览区域
2. 调整纸张、缩放、排版与画笔参数
3. 点击**「开始绘制」**

### 命令行批处理

```bash
# 等比缩放到 A4 绘制
node cli.mjs plot input.svg --paper-size A4 --margin 15

# 按原尺寸 1:1 绘制
node cli.mjs plot input.svg --paper-size A4 --no-fit-page

# 自定义缩放 130% 并裁掉超出绘图区域的部分
node cli.mjs plot input.svg --paper-size A4 --no-fit-page --scale-percent 130 --crop-to-margins
```

### 自定义硬件（自制绘图仪）

在「笔」面板硬件下拉选择「── 新建自定义 ──」，填写步距角、驱动细分、同步轮齿数、齿距与安全工作区宽高（mm），保存后配置持久化到浏览器本地存储，实时换算 `stepsPerMm` 与微步值。

### WebSerial 纯浏览器模式

```bash
cross-env IS_WEB=1 npm run build:ui
```

---

## 8. 适用场景

- **版画/线稿创作** —— 长时绘制大幅作品，需要稳定性保障与漏画补救能力
- **自制绘图仪玩家** —— 自定义传动参数与工作区校验，适配非标准硬件
- **批量/自动化生产** —— CLI 批处理可集成到自动化流水线
- **远程部署** —— 树莓派 + 服务端模式，手机平板随时操控
- **无桌面环境** —— WebSerial 模式免 Node.js，浏览器直连即用

---

## 9. 版本演进与质量保障

项目保持活跃迭代，近期重点版本：

- **v0.20.0**（2026-09-08）—— 缩放模式三态、SVG 真实尺寸自动检测、custom 硬件安全工作区（防撞轴校验 + 预览标红）；修复裁剪后图层过滤索引错位、导入卡死、自定义纸张回显等问题
- **v0.19.0**（2026-09-08）—— 按源文件归档的绘制任务日志、排版设置（九宫格锚点 + 偏移）；修复长动作固定 150s 超时误杀、LM 速率编码溢出、深 FIFO 排空误报、队列错位笔不抬笔、距离统计口径（block 累加 + 步进密度换算）等长时绘制核心问题
- **v0.18.0**（2026-09-07）—— 修复串口写入失败导致服务静默崩溃、复合路径矩阵变换丢失；新增运行日志落盘
- **v0.17.x** —— 归位全流程加固、断连保护、全命令超时、SVG transform 完整支持

完整记录见 [CHANGELOG.md](CHANGELOG.md)，技术细节（回溯算法、EBB 固件行为、接口变更对照）见 [TECHNICAL_REPORT.md](TECHNICAL_REPORT.md)，发布流程见 [docs/RELEASE.md](docs/RELEASE.md)。

质量保障体系：

- **53 个测试用例**（vitest，9 个文件），覆盖回溯/补画归位、超界校验、裁剪过滤、尺寸推算、模拟模式、串口错误恢复等专项；外部样例文件收进 `src/__tests__/fixtures/` 保证发布包内可独立运行
- **biome lint 零告警**（30 文件 0 error / 0 warning）
- 每个版本发布均初始化 git 仓库打 tag（如 `v0.20.0`），发布历史可追溯

---

## 10. 许可证与致谢

**许可证**：AGPL-3.0-only。本项目派生自 [SAXI](https://github.com/alexrudd2/saxi)（AGPL-3.0），基于本项目的修改与分发须遵循该协议的开源义务。

**致谢**：

- [SAXI](https://github.com/alexrudd2/saxi) —— 基础框架（Express + WebSocket 架构、EBB 协议层、恒加速度运动规划、双驱动设计）
- [axi](https://github.com/fogleman/axi) —— 运动规划算法启发
- [Evil Mad Scientist](https://www.evilmadscientist.com/) —— AxiDraw 绘图仪设计者
