# 更新日志（Changelog）

本项目所有值得关注的变更都记录在此文件中。

本文件格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-09-12

> 首个 GRBL 移植版发布。项目派生自姊妹项目 [Bit2AtomPlotWebUI](https://github.com/lymanzhang/Bit2AtomPlotWebUI)（EBB 版，v0.20.0 基线），面向 GRBL 固件体系笔式绘图仪（Z 轴步进电机抬笔）。移植过程与设计决策详见 [GRBL_PORT_PLAN.md](GRBL_PORT_PLAN.md)。

### Added（相对 EBB 基线的新增能力）

- **GRBL 执行层（方案 B）**：主机保留完整 `Plan`（预览/回溯/统计沿用），执行层经 `gcode.ts` 转译为 G-code 流式下发（落笔段 `G1`、空程 `G0`、笔起落 `G1 Z`），速度曲线交由 GRBL planner 规划；`grbl.ts` 行协议层实现握手判型、`ok/error` FIFO 应答匹配、字符计数流控（RX 128 字节窗口，超长行提前拒绝）、`?` 状态解析（v1.1/v0.9 报文）、全命令 15s 超时 + 500ms 沉降期、写失败即中止全部挂起命令
- **Z 轴抬笔后端**：以 Z 轴步进电机替代舵机——`zPenDownMm`/`zPenUpMm`/`zFeedMmMin` 档案参数，UI 笔高滑杆线性映射 Z 行程，抬笔时长按行程与 Z 进给重算
- **G-code 文件导入**：`G0/G1` 直线、`G2/G3` 圆弧（I/J 增量圆心、R 半径式、整圆）、`M3/M4/M5` 与 Z 轴笔控、`G90/G91`、`G20/G21` 英寸换算；N 行号剥离、两类注释、方言参数字静默忽略、未知代码告警跳过；导入后笔画转为路径走 paths→replan 标准管线（预览/回溯/补画/统计/旋转/对齐/缩放全套可用）
- **G-code 成果导出**：消隐/分层/排版后的 Plan 导出为标准 GRBL G-code，文件头注明 `$100–$102` 步进密度与 `$110/$111` 限速建议
- **参数助手**：连接后读取设备 `$$` 与档案换算值（XY/Z 步/mm）逐项对照告警；一键写入 `$100–$102`（白名单校验）；反向同步（设备实值回填档案，推荐方向）
- **固件能力自动探测**：版本横幅 + `$I` 判型（grblHAL 伪装横幅以 `[FIRMWARE:grblHAL]` 为准）、波特率五档轮询、RX 缓冲/`$H`/`$10` 回报格式；优先级 = 用户配置 > 探测值 > 预设档默认
- **GRBL 硬件预设档**：GRBL 1.1 · 丝杆 Z / GRBL 1.1 · 同步带 Z / grblHAL · 丝杆 Z，作为「新建自定义」模板；硬件设置分传动参数（XY/Z）/ 抬笔（Z）/ 固件能力 / 工作区四组持久化
- **内置 GRBL 模拟器**：虚拟设备逐行消费 G-code、planner 深度背压（默认 16 条）、虚拟状态回报、`$H`/`$X`/`$$`/`$I`、可注入 Alarm；支持无硬件全流程（`--driver sim`）
- **Alarm/错误恢复路径**：`ALARM:` 行立即中止挂起命令（不空等超时）；error:1–38 / ALARM:1–9 官方码中文描述与恢复引导；`$H` 归位 / `$X` 解锁恢复端点与 UI
- **软限位协同**：设备开启 `$20` 时绘制前对照 `$130/$131` 行程提前拒绝超界任务；`$20=0` 时提醒开启双保险
- **位置双源校验**：暂停/收尾排空点对照主机跟踪位置与设备 WPos 实测，偏差 >1mm 记录任务日志并按实测修正；`/plot/status` 回报 `penPosKnown`
- **WebSerial 浏览器直连**：与服务端执行/恢复路径同语义（断连保护、Alarm 守卫、取消即时收尾）
- **测试体系**：174 个用例（25 文件，+1 skipped）——含 4 种方言样例「导入→导出→再导入」round-trip 专项（双向点到折线距离 ≤0.002mm + 弧长差校验）、SVG 导出/导入往返几何指纹回归（旋转标记语义、多轮循环稳定）、G-code 导入排版/帧变换回归、协议层 21 用例、模拟器/服务端/直连驱动集成测试；lint 零告警

### Changed（相对 EBB 基线的调整）

- **彻底移除 EBB 兼容路径**：删除 `ebb.ts` 及 EBB 版测试/串口 mock、`--driver ebb` 选项与 `DriverKind` EBB 分支、舵机空间（penPos）逆变换全链路；EBB 设备由姊妹项目 [Bit2AtomPlotWebUI](https://github.com/lymanzhang/Bit2AtomPlotWebUI) 覆盖。`PenMotion` 位置统一为 penPct 口径（0 = 完全抬笔，100 = 完全落笔），GRBL 执行层直接线性映射 Z 高度；硬件预设选中修复为加载对应 GRBL 预设档案
- **毫米口径全程统一**：移除 EBB 版的全步进空间换算与 `X-Plot-Steps-Per-Mm` 请求头，Plan/统计/超界校验全程毫米直读，步/mm 换算交由 GRBL 固件完成
- **设备缓冲模型**：EBB 运动 FIFO 深度窗口 → GRBL 字符计数窗口（发一行扣字节数、收 `ok` 归还）；排空判定改为轮询 `?` 直至 `Idle` 且全部行已 `ok`
- **暂停语义**：停止投喂 + 缓冲区排空确认（与 EBB 版同为路径边界暂停，GRBL RX/planner 缓冲浅，用户可见效果等价）
- **预计时长估算**：按转译后路径长度 ÷ 受档案 `$110–$122` 钳制的有效速度重算（不依赖主机恒加速曲线）

### Fixed

- **载入新文件时排版参数自动复位**：「旋转角度 / 对齐 / 缩放模式」持久化在 localStorage，上一次会话设置的值（如旋转 90°）会在下次读入新文件时被静默套用，表现为图形莫名旋转/偏移/缩放。现每次载入新文件（文件导入与剪贴板粘贴均覆盖）自动复位：旋转 = 0、对齐 = 居中/居中、缩放 = 等比缩放到纸张（fit）；复位后用户仍可手动调整
- **G-code 导入后排版操作无效（旋转/对齐/缩放全部失灵）**：G-code 导入此前解析后直接合成 `Plan` 并 `setPlan`，绕过了 `paths → replan` 管线——而旋转/对齐/缩放等排版变换都在 `replan` 中生效，导致导入后任何排版操作都无作用。修复：解析出的笔画转为符合 flatten-svg 约定的 `Path[]`（机器坐标按当前原点角逐点镜像回屏幕空间，镜像变换自逆），经 `setPaths` 走与 SVG 导入完全相同的管线，排版操作同等生效；逐点帧变换抽取为 `planning.machineFramePoint` 供导出/导入两侧复用。新增 gcode-layout.test.ts 回归（旋转 90° 包围盒纵横比反转、左下原点坐标镜像）
- **导出 SVG 再导入旋转叠加 + 图形缩小（严重）**：两个根因——(1) 导出文件包含白色背景 `<rect>`，重新导入时被 flatten 成一条整页大的笔画路径（图层列表多出 "none" 层），并把 fit 等比缩放的包围盒撑到整页，图形被缩小一圈；(2)「旋转绘制」是规划期变换，导出 SVG 的坐标已含当时的旋转角，重新导入时又按当前旋转选项再应用一次——每导出/导入一圈图形就多转一个角度（实测先顺时针后逆时针各 90°），且旋转后的页面包围盒超出纸面导致 fit 进一步缩小。修复：导出不再输出背景 rect；根节点新增 `data-b2a-rotate-deg` 标记，导入读入 `PlanOptions.bakedRotationDeg`——带标记的文件视为最终排版结果，重导入**不再施加任何旋转**（所见即所得，与当前「旋转角度」取值无关）；用户主动修改「旋转角度」时标记被清除、旋转恢复生效；外部 SVG（无标记）行为不变。新增 svg-roundtrip.test.ts 回归（rotateDrawing=90 往返几何指纹一致、带标记文件忽略当前旋转设置、导出/导入 3 轮循环几何稳定、导出无 rect、无标记文件行为不变）
- **G-code 导入缺逆帧变换（预览镜像 + 模拟起点错位）**：导出 G-code 前经 `applyMachineFrame` 做屏幕→机器变换（如左下原点 = Y 轴镜像），但导入时把机器坐标直接当屏幕坐标显示——预览与原 SVG 沿 X 轴镜像、模拟起点落在屏幕左上而非原点角（penHome 为机器坐标语义，被当屏幕坐标解释）。修复：导入时按当前原点角再应用一次同角帧变换归一化回屏幕空间（镜像变换自逆），首段空程起点随动作序列一并落在原点角。新增 gcode-frame.test.ts：四原点角「导出→导入→逆变换」几何无损往返（笔画数/起终点/折线总长指纹）+ 未归一化 Y 镜像保护用例
- **导出 G-code「无任何反馈」根因（ReferenceError）**：机器帧变换 `machineFramePlan` 此前定义为 PlotButtons 组件私有函数，Root 的导出处理器引用不到作用域——UI 构建走 esbuild（无类型检查）放行，运行时抛 `machineFramePlan is not defined`。已提升为模块级共享函数（PlotButtons 的绘制/补画/归位与 Root 的 G-code 导出共用）；顺带修复 ui.tsx 中 `DriveParams` 组件名与 planning 类型同名导致的类型误用（改用 `OriginCorner`）。**防线加固**：构建链新增 `typecheck` 脚本（tsc 服务端 + UI 双项目 --noEmit），并入 `npm run build`——此类跨组件作用域错误今后在构建期即被拦截
- **SVG 导出笔态判断反转（严重）**：`planToSvg` 仍沿用舵机空间时代的 `finalPos < initialPos`（值小 = 落笔）判断笔态，而 Plan 笔位已统一为 pct 口径（0 = 抬笔、100 = 落笔），方向恰好相反——导出的 SVG 只含空程跳线（1-6mm 短碎片，见 testOutputs/cloud_H_800_1200-export.svg），真正的绘制路径被整批丢弃。已改为 `finalPos > initialPos`，新增 export-svg.test.ts 回归（落笔段导出/空程排除/抬笔方向不置笔态）
- **导出按钮异常显式化**：导出 SVG / 导出 G-code 处理器加 try/catch，异常转为弹窗（此前异常只在浏览器控制台留痕，表现为「点击无任何反馈」）；G-code 导出全链路已用真实文件（cloud_H_800_1200.svg，1710 路径 → 13999 行）验证无服务端异常，若浏览器端仍报错请反馈弹窗中的具体信息
- **无设备启动**：服务端启动时未发现 GRBL 设备不再以错误日志收尾——保持就绪并周期自动探测，设备插入/上电后自动接入（也可 `--device` 指定端口），不要求真机先于服务端上电；串口探测失败不再刷「connection lost / serial port error」噪音日志（握手未完成的端口死亡属探测预期路径，静默处置；主动 close 的 aborted 读错误不记录）
- **补画模式滑块右侧的白点（文字被裁剪）**：`input[type=range]` 作为 flex 子项默认 `min-width: auto` 卡在固有宽度（约 129px）无法收缩，把右侧「第 X 条」条号标签挤出面板边缘裁掉，仅残留一个文字碎片。修复：滑块 `min-width: 0` 允许收缩，标签 `flex: none` + `white-space: nowrap` 保证完整显示（暂停回溯滑块行同样受益）
- **未接机误启绘制**：真实驱动模式下未连接设备时「开始绘制 / 补画」以 409 拒绝并弹窗说明（原先静默回退模拟绘制，用户易误入绘制状态）；无硬件试跑请显式使用 `--driver sim`
- **工作区宽/高无法输入**：输入值改用本地文本态暂存，「先填宽、再填高」的中间态不再被「两维有效才提交」规则立刻清空
- **模拟绘制时的大片空白**：绘图设置面板由 `position: fixed` 改为 `sticky` 参与布局，移除 200px spacer + 150px padding-bottom 占位 hack（内容短于视口时不再露出空白）

### Changed（UI）

- **命名区分**：「更多设置」更名「更多绘制配置」，与「更多设备配置」（机器参数）明确区隔——前者收纳绘制参数（优化/排版/笔运动），后者收纳硬件档案
- **「落下最大速度」更名「绘制进给速度 (mm/s)」并独占一行**（留在「更多绘制配置」）：它是真正生成绘制段 G1 F 进给值的执行参数（受 $110/$111 钳制），与下方改标「落笔参数（仅估算）」「抬笔参数（仅估算）」的时长估算参数明确分层；tooltip 与组说明注明属绘制作业参数而非固件 $ 参数，避免与设备配置里 $110/$111「最大速度」概念混淆
- **绘制进给速度 ↔ $110/$111 联动校验提示**：进给值（×60 → mm/min）超过设备 XY 最大速度最小值时，字段下方说明实时变为红色告警（新 `.field-hint--warn`，明暗主题适配），显示「进给 X mm/min 超过 $110/$111 最小上限 Y，实际将按 Y 执行」；未超限时显示正常灰色说明并回显当前 mm/min 折算值。此前超限只在转译层静默钳制（gcode.ts），用户无从得知改动未生效。边界加固：显示值取整消除浮点噪声、比较加 1e-6 容差（恰等于上限不误报）、$110/$111 ≤0 脏数据视为未配置（gcode.ts 钳制口径同步对齐）
- **$110/$111 配置处档案值说明**：「最大速度 (mm/min)」组下方新增灰色小说明——超限校验与本组上限均以档案配置值为依据（非设备实时 `$$`），设备端手改后请用参数助手「从设备读取」回填
- **新增 PROJECT_INTRODUCTION.md 完整项目文档**（GitHub 发布准备）：全景式技术介绍，覆盖背景与姊妹项目关系、四种运行模式、核心设计（方案 B / 坐标系与机器原点认知一致性 / 字符计数流控 / Z 轴抬笔模型 / 绘制进给与固件限速关系）、模块地图与数据流、绘制执行流程、可靠性纪律清单、UI 结构、测试体系、开发指南与文档索引；README 同步修正过时数据（153→159 个用例）并补「坐标系」配置行
- **落笔/抬笔运动参数语义澄清**：这组参数源自 EBB/AxiDraw 主机全权规划时代；GRBL 方案 B 下实际速度曲线由设备 `$110–$122` 重新规划。保留全部参数，但在「落笔参数」「抬笔参数」组标题下新增灰色小字说明（新 `.field-hint`，明暗主题适配）：落笔最大速度生成 G1 的 F 进给建议值（受 $110/$111 钳制）；加速度/转弯系数仅用于主机预计时长估算；抬笔空程为 G0（按 $110/$111 执行）；抬笔/落笔耗时的实际 Z 速度由设备「Z 进给」决定
- **「更多设备配置」折叠控件**：硬件档案/传动参数/固件能力/坐标系/参数助手/保存与删除配置等机器参数配置功能整体收纳进可折叠面板（`details/summary`，行为与「更多设置」一致），默认收起——画笔设置区只保留笔位、电机操作与重置按钮，UI 更简洁且避免误改参数
- **高权重操作按钮状态配色**：模拟绘制、开始绘制、补画模式三个按钮非激活为蓝色、激活/进行中为红色（新 `.btn-blue`/`.btn-red` 与 `.plot-button` 蓝-红双态，明暗主题各自适配；绘制中按钮 disabled 时红色依然可见），凸显重要性与当前状态
- 按钮布局：导出 SVG / 导出 G-code 同行均分；「笔回原点 / 解锁设备」两处（补画模式入口列与补画面板）改为同行均分；`.button-row` 内按钮统一 `flex: 1` 均分宽度
- **机器原点位置设置**：硬件档案新增「坐标系」组（`DriveParams.originCorner`，缺省左上恒等映射）——设备 (0,0) 所在纸角决定轴方向（左→+X 向右 / 右→向左；上→+Y 向下 / 下→向上）。预览与排版固定屏幕方位（上下左右 = 屏幕视觉方位，预览即所得），绘制/补画/归位/G-code 导出在发送前经 `applyMachineFrame` 统一映射为机器坐标，动作序列与时长不变（补画区间/进度索引一一对应）。解决 CNC 惯例（原点左下、+Y 向上）机器纸面结果与预览上下相反的问题
- **绘制起点跟随机器原点**：`penHome`（起点 x/y）语义改为机器坐标口径——相对「机器原点位置」所选原点角、向纸面内度量，(0,0) 即原点角本身。replan 按原点角换算为屏幕坐标（`massager.ts`），预览起点圈落在原点角，执行层 `applyMachineFrame` 再映射回机器 (penHome)，口径闭环一致。默认 (0,0) 时 $H 归位后笔已在起点处，不再出现从纸面对角斜穿的长程空程（历史行为：起点恒为屏幕左上角）。新增回归测试 `pen-home-origin.test.ts`（四原点角起/终点 + 自定义偏移换算）
- **预览随机器原点联动**：预览标尺刻度改为机器坐标读数（原点在右 → X 值向左递增、在下 → Y 值向上递增），原点角绘制 (0,0) 圈标与 +X/+Y 方向箭头（`--canvas-origin` 主题色，明暗主题各自适配），切换「机器原点位置」即时联动，保证预览排版与真机绘制空间关系认知一致
- 按钮与输入框/下拉框视觉区分：按钮浅灰填充 + 深边框（输入框白底），暗色主题按钮 #3a3a3a vs 输入框 #2d2d2d；按钮文字水平垂直居中（修复 `.flex` 行内 flex 布局按钮文字左对齐）
- 下拉框关闭态选中值与展开选项文字居中（`text-align` + `text-align-last`），设备名称输入框文字居中
- 组标题成对居中排版：传动参数（XY）/ 建议值对照、传动参数（Z）与抬笔 / $102、固件能力 / 自动探测、最大速度、最大加速度、坐标系、参数助手 / 设备 $$ 实值对照等主副题统一居中
- 「重置所有选项」由链接样式（button-link）改回常规按钮，高度与其他按钮一致
- 字号整体再降 1px：正文 13px / 板块标题 15px / 组标题 11px / 副题 9px / 控件 11px / 主绘制按钮 14px；「Z 进给 (mm/min)」标签单独 8px，三列行内不再换行顶下输入框
- 补画/设备操作按钮改为纵向全宽布局（`.button-column`），窄面板中不再中文换行；补画面板滑块行对齐（方位标签/条号列固定宽度，两行滑轨左右对齐）
- 字号整体下调一档并建立三级层级：板块标题 16px / 组标题 12px（破折号说明下沉为 10px 副题换行显示）/ 控件 12px；硬件配置三组参数标题同步拆分主副题；stepsPerMm / Z stepsPerMm 结果行改为主名 + 10px 括号副题两行排版
- **硬件配置行布局统一**：`.flex` 行改列均分 + 8px 间距（修复三列行溢出错位的历史问题），输入框/下拉框统一 28px 高度；「最大速度/最大加速度」改为组头（含单位）+ X/Y/Z 短标签，三列输入框同一水平线对齐

### 传承（自 EBB 版 v0.20.0 基线完整继承）

SVG transform 完整支持、真实物理尺寸检测、三态缩放 + 九宫格排版 + 裁剪至边距、路径排序/合并/过滤、隐藏线去除、暂停回溯重绘、区间补画、防撞轴校验（服务端硬拒绝 + 预览标红）、断连保护、全命令超时、绘制任务日志（按源文件归档）、全中文界面与暗色主题。

## [Unreleased]

- 真实设备验收（4.2）与 `$` 参数推荐基线文档
